# one-cloudkey

One 操作系统官方网站。

域名: [cloudkey.top](https://cloudkey.top)

## 技术栈

- 纯静态 HTML + CSS
- 托管于 Cloudflare Pages
- 无 JavaScript 依赖，零加载延迟

## 本地开发

无需构建，直接编辑 `index.html` 和 `assets/` 下的文件即可。

## 部署

推送到 `main` 分支后，Cloudflare Pages 自动部署。

